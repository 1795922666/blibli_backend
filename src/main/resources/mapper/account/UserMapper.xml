<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zspt.blibli.account.mapper.UserMapper">
    <update id="updateCoinCount" >
        UPDATE bli_user
        SET coin = coin + #{coinCount}
        WHERE id IN (
        SELECT user_id
        FROM bli_videos
        WHERE video_id=#{videoId}
        )
    </update>
    <select id="getLikeCount" resultType="int">
        SELECT  COALESCE(SUM(like_count), 0) FROM bli_videos where user_id=#{userId}
    </select>


</mapper>